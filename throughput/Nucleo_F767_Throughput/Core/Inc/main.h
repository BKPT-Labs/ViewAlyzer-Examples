#pragma once
/* Device header for the ViewAlyzer recorder (it includes "main.h" to reach
 * the CMSIS core + device definitions) and for the application itself. */
#include "stm32f7xx.h"
#include <stdint.h>
#include <stdbool.h>
